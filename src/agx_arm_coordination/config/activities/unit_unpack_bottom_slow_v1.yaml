# Bottom unit, unpack, slow: out of the packing pose over every intermediate
# anchor into the presentation pose. One-way.
#
# Actions: config/catalogue.d/unit_unpack_v1.yaml. Anchors: config/arm_config.yaml.
#
# The one-way half of unit_unpack_slow_v1. Same anchors and the same per-segment
# speed as the fast variant; slow names the path length, not the scaling.
#
# Node 10 moves TO the packing pose rather than assuming it.
#
# Arms only, one serial chain.
activity: unit_unpack_bottom_slow_v1
nodes:
  - { action_no: 10, action_id: both_arms_to_packing_side_v02 }
  - { action_no: 20, action_id: both_arms_to_transit_packing_side_v02 }
  - { action_no: 30, action_id: both_arms_to_transit_functional_init_v02 }
  - { action_no: 40, action_id: both_arms_to_functional_init_hold_v02 }
  - { action_no: 50, action_id: both_arms_to_tee_cup_hold_init_v01 }
edges:
  [ [10,20],
    [20,30],
    [30,40],
    [40,50] ]
